import { SectionWrapper } from "../components/SectionWrapper/SectionWrapper"

export const PageNotFound = () => {
  return (
    <SectionWrapper>
        <h3>Hov, den side du leder efter findes vidst ikke</h3>
    </SectionWrapper>
  )
}